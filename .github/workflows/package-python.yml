name: Package Python

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package-python:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '~1.12.0-rc1'
          arch: x64

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v2

      - name: Build executable and library
        working-directory: juliac
        run: |
          make

      - name: Prepare Python package
        run: |
          mkdir -p python-package
          cp -r poseest_py/* python-package/
          # Remove existing symlinked library (if any)
          rm -rf python-package/poseest/native/RunwayLibCompiled
          # Copy the built library
          cp -r juliac/RunwayLibCompiled python-package/poseest/native/

      - name: Create package archive
        run: |
          cd python-package
          zip -r ../poseest-python-package.zip .

      - name: Upload Python package artifact
        uses: actions/upload-artifact@v4
        with:
          name: poseest-python-package
          path: poseest-python-package.zip
